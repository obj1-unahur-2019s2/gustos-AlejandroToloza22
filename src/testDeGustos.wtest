import personas.*
import objetos.*
import bolichito.*
test "gusto de rosa" {
	bolichito.ponerEnMostrador(munieco)
	bolichito.ponerEnVidriera(pelota)
	assert.equals(true, bolichito.tieneAlgoDeColor(verde))
}